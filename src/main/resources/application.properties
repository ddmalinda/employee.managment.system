spring.application.name=employee.managment.system

# --- MySQL Database Connection ---

# We will create 'abc_company_db' in the next step
spring.datasource.url=jdbc:mysql://localhost:3306/abc_company_db

# The user you created
spring.datasource.username=dasith

# The password for that user
spring.datasource.password=!QAZ1qaz

# --- JPA/Hibernate Settings ---

# This tells Hibernate to update the database tables based on your @Entity classes
spring.jpa.hibernate.ddl-auto=update

# This shows the SQL queries in your console (very useful for debugging)
spring.jpa.show-sql=true